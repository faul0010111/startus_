module.exports = {
  root: true,
  parser: "@typescript-eslint/parser",
  parserOptions: {
    ecmaVersion: 2022,
    sourceType: "module",
  },
  plugins: ["@typescript-eslint"],
  extends: [
    "eslint:recommended",
    "plugin:@typescript-eslint/recommended",
  ],
  ignorePatterns: [
    "dist",
    ".next",
    "node_modules",
    "*.config.*",
    // Generated by `next build` (Next 15 adds a triple-slash *path* reference
    // that trips @typescript-eslint/triple-slash-reference). Next regenerates
    // it on every build, so it must not be linted or hand-edited.
    "next-env.d.ts",
  ],
  rules: {
    "@typescript-eslint/no-explicit-any": "warn",
    "@typescript-eslint/consistent-type-imports": "error",
    "no-console": ["warn", { allow: ["warn", "error", "log"] }],
    eqeqeq: ["error", "smart"],
  },
};
